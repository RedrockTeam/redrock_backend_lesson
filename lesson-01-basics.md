# 第一课：后端协作与 Go 基础

## 这节课讲什么

先用红岩网校后端的真实工作说明“后端”负责请求、业务数据和服务协作，再介绍 Git、GitHub 和 Classroom 的基本工作流。随后从学生已经学过的 C 语言出发，讲 Go 的 `package`、`import`、`var`、基础类型、`func`、`struct`、指针、数组和 slice。

## 课堂安排

- 15 分钟：一次 HTTP 请求如何经过路由、handler 和数据层；
- 20 分钟：clone、branch、commit、push、Pull Request、Actions；
- 35 分钟：C 的函数/结构体/指针与 Go 写法对照；
- 25 分钟：现场写一个学生结构体和平均分函数，运行 `go fmt`、`go test`；
- 20 分钟：完成练习仓库中的基础题；
- 5 分钟：观察线性查找和重复格式化的局限，转到 map/interface。

## 讲解重点

不追求覆盖全部语法。每个语法点都通过一个能测试的函数出现；强调 Go 的包可见性、零值、错误返回和 slice 的长度/容量。Git 演示只保留学生本课真正会用到的命令。
