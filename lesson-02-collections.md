# 第二课：map 与 interface

## 这节课讲什么

从第一课的线性查找开始，说明 map 如何按 key 管理成绩；再用方法和隐式 interface 把“数据”和“输出方式”分开。补充 map 的零值、缺失 key、删除和无序遍历。

## 课堂安排

- 15 分钟：复盘线性查找的复杂度和重复代码；
- 30 分钟：创建、读取、更新、删除 map，处理 `value, ok`；
- 25 分钟：方法、`Stringer` 风格接口和错误处理；
- 25 分钟：把成绩榜改成 map，并支持文本/JSON 两种展示；
- 20 分钟：运行公开测试，第一次运行 `go test -race`；
- 5 分钟：看见并发读写 map 的失败，预告 goroutine。

## 讲解重点

接口只写调用方真正需要的行为；不要为了抽象而抽象。让学生亲自看到 map 遍历顺序不稳定，以及普通测试“看起来通过”不等于并发安全。
